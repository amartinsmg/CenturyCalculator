{
  "scripts": {
    "build": "npm run build:check && npm run build:prod",
    "build:check": "tsc",
    "build:prod": "babel Src -d Dist --out-file-extension .min.js --extensions .ts -D --minified --no-comments"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.9",
    "@babel/preset-env": "^7.16.11",
    "@babel/preset-typescript": "^7.16.7",
    "typescript": "^4.6.3"
  }
}
